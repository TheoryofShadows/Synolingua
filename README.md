# Synolingua

Early prototype of a Spanish learning app built with Vite and React.

## Status

Work in progress — not production-ready. UI and learning flows are experimental.

## Local development

```bash
npm install
npm run dev
```

Build: `npm run build` · Preview: `npm run preview`
